[build]
  publish = "."
  command = "node -e 'const fs=require(\"fs\"); const key=process.env.PIXABAY_API_KEY; if(!key){console.error(\"PIXABAY_API_KEY is missing\"); process.exit(1)} fs.writeFileSync(\"config.js\", \"export const API_KEY = \" + JSON.stringify(key) + \";\\n\");'"
